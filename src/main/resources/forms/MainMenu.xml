<ui-menu-bar>
    <ui-menu text="{File}" var="file-menu">
        <ui-menu-item icon="images/icons/project16.png" text="{New Project}" var="menu-new-project" />
        <ui-menu-item text="{Open Project}" var="menu-open-project" icon="images/icons/open16.png" />
        <separator />
        <ui-menu-item icon="images/icons/save16.png" text="{Save All}" var="menu-save-all" />
        <ui-menu-item icon="images/icons/settings16.png" text="{Settings}" />
        <separator />
        <ui-menu-item text="{Exit}" />
    </ui-menu>
    <ui-menu text="{Edit}">
        <ui-menu-item text="{Undo}" icon="images/icons/undo16.png" />
        <ui-menu-item text="{Redo}" icon="images/icons/redo16.png" />
        <separator />

    </ui-menu>
    <ui-menu text="{Build}">
        <ui-menu-item text="{Run}" icon="images/icons/play16.png" />
    </ui-menu>
    <ui-menu text="{Tools}">
        <ui-menu-item text="{Simple tool}" />
    </ui-menu>
</ui-menu-bar>
