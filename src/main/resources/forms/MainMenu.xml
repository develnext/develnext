<ui-menu-bar>
    <ui-menu text="{File}">
        <ui-menu-item text="{New Project}" />
    </ui-menu>
    <ui-menu text="{Edit}">
        <ui-menu-item text="{Undo}" />
        <ui-menu-item text="{Redo}" />
    </ui-menu>
</ui-menu-bar>